import React from 'react';
import ReactDOM from "react-dom/client"
import App from "./version4/component/App"

ReactDOM 
      .createRoot(document.getElementById('root'))
      .render(
            <App/>
      )